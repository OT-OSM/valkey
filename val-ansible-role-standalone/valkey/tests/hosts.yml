all:
  children:
    valkey_standalone:
      hosts:
        standalone:
          ansible_host: <publip-ip>
          ansible_user: ubuntu
          ansible_ssh_private_key_file: path/to/private/key
  
