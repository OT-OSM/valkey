# tasks/logdir.yml
- name: Create log directory for Valkey
  file:
    path: "{{ valkey_log_file | dirname }}"
    state: directory
    owner: "{{ valkey_user }}"
    group: "{{ valkey_user }}"
    mode: '0755'
  