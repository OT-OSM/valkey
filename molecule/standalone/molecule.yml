---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: valkey-standalone-instance
    image: ubuntu:22.04
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
  lint:
    name: ansible-lint

verifier:
  name: ansible
